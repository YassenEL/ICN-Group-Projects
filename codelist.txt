//Provides the list of program files and/or scripts. Each line provvides the name of one file and one sentence of description.
traceroute.py  // This is the main file in order to run the traceroute at each building, and get the output to read and outputs a graph that represents the network topology at that location in Howard's WiFi.
topologygraph.py // This file parses through all the txt outputs, and then produces the topology graph given all the IP addresses within Howard's network.
